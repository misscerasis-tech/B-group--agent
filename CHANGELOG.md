# Changelog

所有重要变更都会记录在本文件中。

格式参考 Keep a Changelog，版本号遵循“阶段可用版本”的语义。

## [Unreleased]

### Added

- 增加 B 组工作助手开发分支 `feature/b-group-working-assistant`。
- 增加 B 组固定启动脚本 `dev:b`，默认启动 3002 端口。
- 本地诊断脚本会识别 `3002` 端口占用进程，并建议下一个可用端口，减少 A/B/C 组服务串台。
- 增加 `/api/health` 健康检查接口，用于确认 Web 和数据库连接状态。
- 更新 README 和架构文档，移除数据复盘与集成设置仍是占位的旧说明。
- 更新 B 组能力拆分文档，使已落地能力和待补齐能力与当前实现一致。
- 增加 B 组工作助手开发日志，记录当前分支、环境限制、已推送能力和验证命令。
- `.env.example` 将默认 AI Provider 改为 `local-rule`，避免误认为当前版本绑定真实模型供应商。
- 增加 Agent 对话、结构化操作、产品事实、项目策略、内容计划、素材包、提醒和变更日志数据模型。
- 增加 B 组本地规则型中文指令解析器，可识别市场、渠道增删、素材包频率和内容方向。
- `/b-agent` 升级为数据库驱动的中文工作助手，可提交指令、确认策略、确认应用变更、生成首月计划。
- `/b-agent` 空项目状态支持直接用中文 Brief 启动第一个 B 组项目。
- 产品大脑支持从产品说明生成初始产品事实、手动新增事实和确认事实。
- 增加 Asset、ImageGenerationProviderConfig、ImageGenerationJob 数据模型和 migration。
- 增加素材库 `/assets`，支持本地上传素材、关联项目/产品、保存审核状态和文件元数据。
- 增加图片生成候选供应商和模板化合成任务 seed。
- 增加 IntegrationConnection、IntegrationMigrationRecord 数据模型和 migration。
- 集成设置页面支持保存飞书占位连接、停用旧连接和记录迁移说明，不接真实飞书。
- 集成设置支持飞书占位连接测试，并将创建、停用、测试和迁移记录写入变更日志。
- 内容日历、素材包中心、审核中心、提醒中心改为读取当前 Workspace 真实数据。
- 首月计划生成时写入未来 4 周截止日期，并在 B 组 Agent、内容日历和导出中展示。
- 内容日历支持手动新增计划项，并自动挂到项目最新策略和变更日志。
- 增加 ReviewTask 数据模型、审核任务生成和审核通过/要求修改的 Web-first 审核流。
- 提醒中心支持根据当前 Workspace 数据生成主动提醒，并可标记完成或忽略。
- 主动提醒扫描会根据近期渠道表现生成点击率偏低、有点击无转化、有花费无转化等数据风险提醒。
- 提醒中心支持手动创建 Workspace 或项目级提醒，并记录级别、截止日期和变更日志。
- 素材包中心支持按当前 Workspace 下载本地 ZIP 可交付文件包。
- 素材包中心支持手动创建素材包结构，并自动生成 V1 默认文件清单。
- 素材包可从素材包中心或 `/b-agent` 提交 Web 审核，自动生成审核任务并记录变更日志。
- 素材包文件项支持关联当前 Workspace 下已审核素材，关联后自动进入已生成状态并记录变更日志。
- 素材包 ZIP 导出的 README 与 manifest 会展示已关联素材名称、类型、来源和校验信息。
- 素材包 ZIP 导出的 manifest 增加策略、产品事实、内容计划、已审核素材和可交付性检查摘要，便于复盘和迁移核对。
- 素材包 ZIP 导出升级为本地可交付文件包，包含基础 PDF、XLSX、DOCX、TXT、关联素材和 manifest。
- 默认素材包文件清单补齐“海报文案 DOCX”，新建和 Agent 生成素材包均保持 11 项 V1 结构。
- `/b-agent` 展示项目策略版本历史，便于查看确认后变更产生的新版本。
- 增加 MetricsSnapshot 数据模型，数据复盘支持手动录入渠道表现并聚合曝光、点击、转化和花费。
- 数据复盘增加 CTR、点击转化率、单次转化成本，并校验点击/转化不能超过上游指标。
- 数据复盘支持粘贴 CSV 或表格文本批量导入渠道表现，导入时按当前 Workspace 校验项目并写入变更日志。
- 产品大脑增强本地规则型事实提取，可从补充资料识别卖点、规格、场景、人群和合规注意。
- 产品大脑支持从已关联的 TXT、MD、CSV、JSON、DOCX 和文本型 PDF 产品资料 Asset 提取待确认产品事实；扫描件 PDF 会要求先 OCR 或整理成 Brief。
- 内容日历支持更新计划项状态，并将计划推进写入变更日志。
- 素材包中心支持推进文件项状态，并自动更新素材包整体状态。
- Seed 增加第二个演示 Workspace，用于直接验证多 Workspace 数据隔离。
- Seed 会写入本地 SVG 示例产品图、Logo 和模板海报，并把演示素材包海报文件项关联到生成图。
- 素材库上传会校验关联项目/产品的 Workspace 归属，并支持审核通过或拒绝素材。
- 增加 B 组工作助手验收清单文档。
- `/b-agent` 待确认 Agent 操作支持拒绝，拒绝后不修改正式策略并写入变更日志。
- 规则型中文指令解析支持新增/删除客群，以及删除内容方向。
- 规则型中文指令解析支持修改当前项目状态，并真实写入 Project 表。
- B 组 Agent 对话支持创建项目提醒，适合记录奖品、合规、Logo 和素材来源待办。
- B 组 Agent 对话支持通过中文指令生成首月计划和第一份素材包结构。
- B 组 Agent 对话支持通过中文指令录入渠道表现指标，并写入数据复盘和变更日志。
- 今日工作台和项目详情增加项目就绪度体检，按产品、事实、策略、计划、素材、审核、复盘和提醒生成评分与下一步动作。
- 项目详情支持将项目体检缺口一键生成项目提醒，并避免重复创建同标题开放提醒。
- 新增 `/setup` 中文本地运行诊断页，并增强 `/api/health` 返回环境、数据库和下一步排障建议。
- 素材包中心增加可交付性检查，导出 ZIP 时包含 `00-交付检查.txt`，并明确生成图不能替代真实产品图和官方 Logo。
- Agent 策略修改增强风险提示，可识别删除唯一渠道、删除仍被内容日历使用的渠道、清空内容方向、降低素材包频率和暂停/归档项目等风险。
- Seed 增加“蒙古夏季预热内容缺口项目”，用于演示项目体检阻塞、缺口提醒和素材包可交付性检查。
- 今日工作台新增 Workspace 安全备份导出，`/workspace/export` 可下载当前 Workspace 结构化 JSON，递归屏蔽疑似密钥字段。
- 增加 `backup:validate` 本地备份校验命令，可检查 Workspace 备份 JSON 的 schema、计数、敏感字段脱敏和 Workspace 归属。
- 今日工作台新增 Agent 工作简报，基于素材包、近期计划、开放提醒和渠道表现生成中文重点进展、风险和下一步动作。
- 增加 Agent 中文可执行能力目录，`/b-agent` 页面从统一清单渲染指令示例，避免未来接 GPT 时能力散落。
- B 组 Agent 支持通过中文指令新增待复核产品事实，写入当前项目关联产品并进入产品大脑确认流程。
- B 组 Agent 支持通过中文指令校准已有产品事实，更新后自动转为需复核并记录变更日志。
- B 组 Agent 支持从对话中粘贴的中文产品资料批量提取待复核产品事实，已确认事实不会被静默覆盖。
- B 组 Agent 支持通过中文指令确认当前项目关联产品的待复核/草稿事实。
- B 组 Agent 支持根据当前项目产品事实生成本地规则型策略推荐草案；已有正式策略时会创建新的草案版本。
- B 组 Agent 支持通过中文指令新增内容日历计划项，并写入变更日志。
- B 组 Agent 支持通过中文指令把内容日历计划项改为需审核、可执行或草稿状态，并写入变更日志。
- B 组 Agent 支持通过中文指令调整内容日历计划项截止日期，并写入变更日志。
- B 组 Agent 支持通过中文指令完成项目提醒和内容日历计划项，找不到匹配目标时会失败并避免误写入。
- B 组 Agent 创建项目提醒时支持解析明确截止日期并写入 `Reminder.dueAt`。
- B 组 Agent 支持通过中文指令忽略或取消开放提醒，并写入 `DISMISSED` 状态和变更日志。
- B 组 Agent 支持通过中文指令调整开放提醒截止日期，并写入变更日志。
- B 组 Agent 支持把当前策略渠道和首月周次的内容日历缺口转换为提醒，避免策略渠道没有排期。
- B 组 Agent 支持把当前项目数据复盘风险转换为提醒，复用低点击率、无转化和投放花费风险判断。
- B 组 Agent 支持把项目体检缺口转换为当前项目提醒，复用项目就绪度算法并避免重复开放提醒。
- B 组 Agent 支持通过中文指令创建指定周期素材包结构，并自动生成 V1 默认 11 项文件清单。
- B 组 Agent 支持通过中文指令归档素材包或恢复为草稿，并写入变更日志。
- B 组 Agent 支持把素材包可交付性阻塞项转换为提醒，覆盖文件生成和真实产品图/Logo 缺口。
- B 组 Agent 支持在用户明确确认后批量推进素材包文件项状态，并同步更新素材包整体状态。
- B 组 Agent 支持把当前项目最新已审核模板海报 Asset 关联到最新素材包的海报文件项。
- B 组 Agent 支持通过中文指令提交素材包审核，自动更新素材包状态并创建缺失审核任务。
- B 组 Agent 支持通过中文指令处理素材包审核通过或要求修改，只匹配当前项目下已有待审核任务。
- B 组 Agent 支持通过中文指令处理审核中心中的产品事实、策略草案和素材审核任务，并同步写回对应业务对象。
- B 组 Agent 支持通过中文指令取消当前项目下的待审核任务，不修改被审核业务对象。
- B 组 Agent 支持补齐当前项目审核任务，覆盖策略草案、素材包、待审核素材和待复核产品事实。
- `/b-agent` 增加常用流程按钮，可一键执行策略推荐、首月计划、项目体检提醒、日历缺口提醒、审核任务补齐和素材包缺口提醒。
- `/b-agent` 右侧工作台增加素材与审核状态，按当前 Workspace 和项目展示真实产品图、官方 Logo、生成图和待处理审核任务。
- `/b-agent` 待审核任务支持直接通过、要求修改或取消，服务端按当前 Workspace 和项目限制处理范围。
- `/b-agent` 主动提醒区支持直接完成或忽略开放提醒，处理后留在当前项目工作台并写入变更日志。
- `/b-agent` 在已有项目时增加中文 Brief 新项目启动入口，提交后自动进入新项目工作台。
- B 组 Agent 中文指令支持启动新项目，自动创建项目、产品、产品事实、策略草案、对话和操作记录，并跳转到新项目工作台。
- B 组 Agent 中文指令支持给当前项目新增并关联产品，优先复用当前 Workspace 同名产品，并为新产品生成待确认事实。
- `/b-agent` 右侧工作台顶部增加项目就绪度体检，直接展示评分、阻塞状态和下一步动作。
- `smoke` 冒烟检查支持同一路由的多个可接受中文标题，避免 `/b-agent` 数据正常和降级状态下标题不同导致误报。
- `/b-agent` 素材包预览文案更新为当前可下载基础 ZIP 交付包，避免误导为仍是占位。
- `/b-agent` 素材包预览增加交付体检，复用素材包中心同一套可交付性评分、阻塞项和下一步动作。
- 增加 `smoke` 冒烟检查脚本，可检查 `/setup`、`/api/health`、`/b-agent` 和 `/dashboard` 是否在指定端口响应。
- 新增 `prisma.config.ts`，将 Prisma seed 配置从弃用的 `package.json#prisma` 迁移到 Prisma 配置文件。
- 增加 `AgentTextProvider` 抽象，当前通过 `local-rule` Provider 解析中文指令，未来真实 GPT 接入不需要改业务写入路径。
- 审核中心支持取消误生成或暂不需要处理的待审核任务。
- 顶部 Workspace 区域支持创建新的 Workspace，并自动切换到新 Workspace。
- 项目和产品的创建、编辑、产品关联、事实生成与确认会写入变更日志，便于复盘。
- 素材上传后自动创建素材审核任务，避免真实产品图和官方 Logo 绕过审核中心。
- 素材库直接审核或拒绝素材时，会同步关闭对应审核任务。
- 素材上传增加 20MB 大小限制和按素材类型的文件格式校验。
- 素材库支持按当前 Workspace 下载本地上传的原始文件，并增加本地路径安全校验。
- 素材库支持基于已审核真实产品图和官方 Logo 生成本地分层 SVG 模板海报，结果进入统一 Asset，不调用外部图片模型。
- 增加图片生成产品主体锁定护栏测试，防止未来供应商适配器允许重绘产品层。
- 增加项目工作台 JSON 快照导出，覆盖项目、产品事实、策略、计划、素材包、提醒、审核和变更日志。
- 数据复盘页面改为基于系统内策略、计划、素材、素材包、提醒、指令和变更日志生成摘要。
- 数据复盘支持导出当前 Workspace 的 JSON 快照，包含指标、可用性信号、建议、项目、近期表现和最近变更。
- 正式策略确认后的 Agent 变更会创建新的策略版本，旧版本保留用于复盘和回滚。
- 今日工作台增加内容计划、素材包、待处理提醒、最近变更摘要和按优先级生成的今日行动队列。
- 项目中心增加中文 Brief 启动项目能力，一次创建项目、产品、关联、待确认事实、策略草案、对话和变更日志。
- 增加 B 组工作助手 Prisma migration 和增强 seed 数据。
- 增加中文指令解析单元测试。
- 增加 C 组 CES 项目推进 Agent 工作台 `/c-agent`。
- 增加 CES 项目启动指令解析、项目快照、WBS、v1/v2/v3 版本节奏、计划疏漏检查和待 Owner 验证问题。
- 增加 CES 项目飞书文档草案、WBS 任务表行数据和 Owner 确认清单导出工具。
- 增加 C 组 CES 项目推进 Agent 产品说明文档。
- 增加 C 组飞书文档、任务表、群聊、日历和审批的落地契约文档。
- 增加 C 组项目 Owner 待确认清单文档。
- 增加 C 组项目计划生成与审查逻辑测试。
- 增加 B 组能力拆分与演示链路计划文档。
- 优化 `/b-agent` 为“左侧中文 AI 对话 + 右侧结构化项目工作台”的正式演示入口。
- `/b-agent` 使用模拟数据展示产品事实提取、策略推荐、人工确认、首月计划和素材包预览。
- 增加 AI 赛 ABC 三组路由拆分规则，避免 `/agent` 被单一小组覆盖。
- 增加 B 组固定入口 `/b-agent`。
- 增加通用分组入口 `/agent`。
- 搭建 Next.js + TypeScript 中文后台系统骨架。
- 增加左侧导航、顶部 Workspace 切换和右侧内容区。
- 增加 Docker PostgreSQL 本地开发配置。
- 增加 Prisma schema、Foundation migration 和可重复 seed。
- 增加 User、Workspace、WorkspaceMember、Project、Product、ProjectProduct 最小模型。
- 增加 Workspace 级服务端数据隔离工具和基础测试。
- 增加项目创建、查看、编辑和关联产品能力。
- 增加产品创建、查看和编辑能力。
- 增加今日工作台、项目中心、产品大脑和 V1 导航占位路由。
- 增加可插拔图片生成接口和图层化视觉策略文档。

### Changed

- 更新 README、架构、开发计划和回滚文档，补充第一阶段启动、数据库和验证说明。

## [v0.0.1-initial] - 2026-07-17

### Added

- 初始化独立项目目录和 Git 仓库。
- 建立 `main` 稳定分支规则。
- 创建基础文件：`README.md`、`CHANGELOG.md`、`.gitignore`、`.env.example`。
- 创建规划文档：`docs/architecture.md`、`docs/development-plan.md`、`docs/rollback-guide.md`。
- 固化 Web-first、Feishu-connected 的架构原则。
- 固化阶段开发、数据库迁移、凭证管理和回滚原则。
